[build]
  command = "npm run build"
  publish = "dist"

[[plugins]]
  package = "netlify-plugin-submit-sitemap"

  [plugins.inputs]
    baseUrl = "https://yakub.blog"
    sitemapPath = "/sitemap-index.xml"
    providers = ["google"]
